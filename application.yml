spring:
  datasource:
    url: jdbc:${DATABASE_URL:postgresql://localhost:5432/myblog}
    username: postgres
    password:
    driver-class-name: org.postgresql.Driver
  jpa:
    generate-ddl: false
    show-sql: false
    hibernate:
      ddl-auto: validate
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB
      enabled: true

upload:
  path: /Users/alex/Desktop/upload

server:
  port: ${PORT:8080}

appEmail:
  email: javaentwickler20@gmail.com
  password: Sjeu#94Tn1Uaq83z

captchaExpirationTime: 1




